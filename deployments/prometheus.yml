global:
  scrape_interval: 15s

scrape_configs:
  - job_name: "prometheus"
    static_configs:
      - targets: ["prometheus:9090"]

  - job_name: "logstorm"
    static_configs:
      - targets: ["host.docker.internal:3123"] # LogStorm server
    metrics_path: /metrics
    scrape_interval: 10s
